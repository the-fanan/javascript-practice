// IDENTIFIER is a name used for variables or functions
var _identifier;
var $identifier;
var letterIdentifier;

;(2+1) - 9//put a semicolon before brackets to avoid it being continued with previous line

/**
 * For the following identifiers, if the there is a line break after them, it will be treated as a semi-colon
 * 
 *  return
 *  break
 *  continue
 */
var
a
=
5

console.log(a)
// ++ and -- are used as prefix to statement that follows